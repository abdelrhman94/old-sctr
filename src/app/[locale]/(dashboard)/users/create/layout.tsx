import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Sub User",
  description: ""
};

export default function CreateSubUserLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
